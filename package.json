{
  "name": "token-system",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack Token Management System with localStorage and Vercel deployment support",
  "scripts": {
    "build": "npm --prefix client run build",
    "vercel-build": "npm --prefix client run build",
    "dev": "npm --prefix client run dev"
  }
}
