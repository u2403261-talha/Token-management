{
  "name": "token-system",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack Token Management System configured for Vercel deployment",
  "scripts": {
    "build": "npm --prefix client install && npm --prefix client run build",
    "vercel-build": "npm --prefix client install && npm --prefix client run build",
    "server": "npm --prefix server start",
    "client": "npm --prefix client dev"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.3.4"
  }
}
